<%
	
	record = this.model.find(params[:id])
	admit_id = record.admit_id
	
	
	
	record.destroy
   if admit_id	
%><%=redirect_to '../Admit/show?id='+admit_id %><% else %>
<%=redirect_to 'index' %><% end %>