<%
zone = Zone.find session[:current_zone]
if params[:ambu_id]
ambu = Ambulance.find(params[:ambu_id])
session[:current_ambu] = ambu.id
end
%>
<script>
if (typeof device_interface !== "undefined") {
	device_interface.setParams("vehicle_id", ambu.name)
}
</script>
<meta http-equiv="Refresh" content="0; url='index'" />
