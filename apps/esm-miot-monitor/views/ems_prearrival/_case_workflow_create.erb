<% if request.post? 


	
if params[:record]
# if params[:case_id] and params[:kworkflow_id]

	p = params[:record]
	record = EMSCaseWorkflow.create p
	
	# record = EMSCaseWorkflow.create :case_id=>params[:case_id],:kworkflow_id=>params[:kworkflow_id]
			
end
	


%>
<!--<META HTTP-EQUIV="Refresh" CONTENT="0;URL=show?id=<%=record.id%>">-->
<% if record%>
บันทึกสำเร็จ

<% end #if record%>

<% end #if request%>

<script>
	//refresh_remote()
</script>