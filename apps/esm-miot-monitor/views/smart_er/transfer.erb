<%
	
if params[:id]
	
	admit = Admit.find params[:id] 
	
	if params[:bed_id]
		
		bed = Bed.find params[:bed_id]
		room = Room.find bed.room_id
		admit.update_attributes :bed_id => params[:bed_id], :room_id=> bed.room_id,  :room_no=>room.name, :bed_no =>bed.name 
	
	elsif params[:room_id]
		
		room = Bed.find params[:room_id]
		admit.update_attributes :room_id=> room.id, :room_no=>room.name
		
	
	end
	
	
%>

<META HTTP-EQUIV="Refresh" CONTENT="0;URL=index">
<% end %>