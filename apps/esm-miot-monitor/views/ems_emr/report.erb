

<%

     doc = EMRDocument.where(:name=>'patient_assessment').first

     # records = EMRRecord.where(:document_id=>doc.id).all

      records =  EMRRecord.where(:document_id=>doc.id, 'data.trauma_status'=>'trauma').all

%>

<%=records.to_json%>
