<%
    if params[:id]
     
            # ค้นหา ERCase
            er_case = ERCase.find_by(id: params[:id])
            
            if er_case
                # ลบ Admit ที่เกี่ยวข้อง
                admit = Admit.find_by(er_case_id: er_case.id)
                admit.destroy if admit
                
                # ลบ ERCase
                er_case.destroy
                
                # แสดงข้อความสำเร็จ (ถ้าต้องการ)
                # flash[:success] = "ลบข้อมูลสำเร็จ"
            end
 
    end
    
    # redirect กลับไปหน้า list
         
%>
   %>
                <meta http-equiv="refresh" content="0; url=list" />
                <%