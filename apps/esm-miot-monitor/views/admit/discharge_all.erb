<%

	@current_zone = nil

	if session[:current_zone]
	
		@current_zone = Zone.find session[:current_zone] 
	
		if @current_zone
				
			admits = Admit.where(:zone_id=>@current_zone.id).all
			
			for admit in admits 
					admit.discharge
			end
			
		end
	
	end


	
	
	
%>
Discharge all records.... <%=admits.size%>
<%#redirect_to "index"%>
