<%
res = Provider.new
if params[:user_id]
	
	line_account = LineAccount.where(:user_id=>params[:user_id]).first
	
	
	if line_account and provider = Provider.where(:line_account_id=>line_account.id).first
	
		res = provider 
		
	end
		
		
end	
%><%=res.to_json%>